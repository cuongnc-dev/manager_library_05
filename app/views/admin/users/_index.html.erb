<tr>
  <td><%= col-serial user_counter %></td>
  <td><%= name_limit user.name %></td>
  <td><%= name_limit user.email %></td>
  <td><%= user.phone %></td>
  <td><%= address_limit user.address %></td>
  <td>
    <% if user.is_admin? %>
      <%= t "admin" %>
    <% else %>
      <%= t "user" %>
    <% end %>
  </td>
  <td><%= user.activated %></td>
  <td class="image">
    <%= image_tag user.avatar, alt: t("users.image_alt") %>
    <div class="edit-delete">
      <%= link_to "", edit_admin_user_path(user), remote: true,
        class: "glyphicon glyphicon-pencil" %>
    </div>
  </td>
</tr>
